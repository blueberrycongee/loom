// LoomUI — SwiftUI views. Thin layer over LoomCompose + LoomDesign.
// Each screen owns its own ViewModel; app-level state lives in the
// executable target.

import Foundation

public enum LoomUI {}
