// LoomCompose — orchestration. Turns an index + a style + a seed into a
// Wall, by: clustering candidates on the active axis (color/mood/scene),
// sampling a shortlist under aesthetic constraints, then letting the chosen
// LayoutEngine place them.

import Foundation

public enum LoomCompose {}
