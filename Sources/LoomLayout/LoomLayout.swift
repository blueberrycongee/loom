// LoomLayout — layout engines (Tapestry, Editorial, Gallery, Collage,
// Minimal, Vintage) and the aesthetic scoring that constrains their output.
//
// An engine takes a shortlist of Photos and a seeded RNG and emits a Wall of
// placed Tiles. Aesthetic scorers grade candidate walls so the composer can
// reject misses.

import Foundation

public enum LoomLayout {}
