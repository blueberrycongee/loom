// LoomCore — domain types and small utilities shared across modules.
// Keep this layer free of platform (AppKit/SwiftUI/Vision/Core Image) imports
// so it can be unit-tested on any Apple platform and remain the stable center
// of the architecture.

import Foundation

public enum LoomCore {
    public static let version = "0.1.0"
}
